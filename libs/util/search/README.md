# util-search

Recherche approximative — le _fuzzy match_ de toutes les recherches de
l'application, et le découpage qui permet d'en surligner les correspondances.

## Running unit tests

Run `nx test util-search` to execute the unit tests.
